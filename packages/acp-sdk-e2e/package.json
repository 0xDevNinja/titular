{
  "name": "@titular/acp-sdk-e2e",
  "version": "0.0.0-private",
  "private": true,
  "description": "Integration tests for @titular/acp-sdk against a real anvil chain, Postgres-backed gateway-go, and (best-effort) the indexer pipeline. Lives in its own package so the SDK's published tarball stays free of integration deps.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "biome check src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "compile:mocks": "node ./scripts/compile-mocks.mjs"
  },
  "dependencies": {
    "@titular/acp-sdk": "workspace:*",
    "pg": "^8.13.0",
    "testcontainers": "^10.13.0",
    "viem": "^2.21.50"
  },
  "devDependencies": {
    "@types/node": "~22.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "~5.6.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
